dependencies {
	compile group: 'org.apache.commons', name: 'commons-compress', version: '1.13'
}

android {  
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.nativescript.docbrowse"  
  }  
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
